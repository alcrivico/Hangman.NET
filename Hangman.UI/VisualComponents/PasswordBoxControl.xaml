<UserControl
    x:Class="Hangman.UI.VisualComponents.PasswordBoxControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Hangman.UI.VisualComponents"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="PasswordBox"
    d:DesignHeight="{Binding PasswordBoxHeight, ElementName=PasswordBox}"
    d:DesignWidth="{Binding PasswordBoxWidth, ElementName=PasswordBox}"
    mc:Ignorable="d">

    <Grid>

        <TextBlock
            x:Name="PasswordBox_FieldName"
            Margin="15,0,0,0"
            Padding="5,0,5,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Panel.ZIndex="1"
            Background="{StaticResource SolidColorBrush_MarianBlue}"
            FontFamily="{StaticResource Font_Nunito}"
            FontSize="12"
            FontWeight="Bold"
            Foreground="{StaticResource SolidColorBrush_Gold}"
            Text="{Binding FieldName, ElementName=PasswordBox}" />

        <Border
            x:Name="TextBox_Border"
            Height="40"
            Panel.ZIndex="0"
            Background="{StaticResource SolidColorBrush_MarianBlue}"
            BorderBrush="{StaticResource SolidColorBrush_Gold}"
            BorderThickness="3"
            CornerRadius="10">

            <PasswordBox
                x:Name="PasswordBox_Text"
                Padding="10,0,10,0"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                Background="Transparent"
                BorderThickness="0"
                CaretBrush="{StaticResource SolidColorBrush_Gold}"
                FontFamily="{StaticResource Font_Nunito}"
                FontSize="18"
                FontWeight="Bold"
                Foreground="{StaticResource SolidColorBrush_White}"
                Password=""
                PasswordChar="*" />

        </Border>

    </Grid>

</UserControl>
